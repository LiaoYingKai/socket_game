import React, { useState, } from 'react';
import PropTypes from 'prop-types';

const propTypes = {};

function Main() {
	return (
		<div>
			Hello World
		</div>
	);
}

Main.propTypes = propTypes;

export default Main;
